name: Alpine

on:
  push:
    paths:
      - .github/workflows/alpine.yml
      - shell/**
      - .dockerignore
      - alpine.Dockerfile
  pull_request:
    paths:
      - .github/workflows/alpine.yml
      - shell/**
      - .dockerignore
      - alpine.Dockerfile

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      DOCKER_BUILDKIT: "1"
    steps:
      - uses: actions/checkout@v4.1.1

      - name: Build image
        run: docker build -f alpine.Dockerfile -t kbuley/godevcontainer .

  publish:
    needs: [verify]
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.1.1

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: kbuley
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - uses: docker/login-action@v3
        with:
          registry: quay.io
          username: kbuley+github
          password: ${{ secrets.QUAY_PASSWORD }}

      - name: Set variables
        id: vars
        run: |
          BRANCH=${GITHUB_REF#refs/heads/}
          TAG=${GITHUB_REF#refs/tags/}
          echo "commit=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
          echo "created=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_OUTPUT
          if [ "$TAG" != "$GITHUB_REF" ]; then
            echo "version=$TAG" >> $GITHUB_OUTPUT
            echo "alpine_version=$TAG-alpine" >> $GITHUB_OUTPUT
            echo "platforms=linux/amd64" >> $GITHUB_OUTPUT
          elif [ "$BRANCH" = "main" ]; then
            echo "version=latest" >> $GITHUB_OUTPUT
            echo "alpine_version=alpine" >> $GITHUB_OUTPUT
            echo "platforms=linux/amd64" >> $GITHUB_OUTPUT
          else
            echo "version=$BRANCH" >> $GITHUB_OUTPUT
            echo "alpine_version=$BRANCH-alpine" >> $GITHUB_OUTPUT
            echo "platforms=linux/amd64" >> $GITHUB_OUTPUT
          fi

      - name: Build and push final image
        uses: docker/build-push-action@v5
        with:
          file: alpine.Dockerfile
          platforms: ${{ steps.vars.outputs.platforms }}
          build-args: |
            CREATED=${{ steps.vars.outputs.created }}
            COMMIT=${{ steps.vars.outputs.commit }}
            VERSION=${{ steps.vars.outputs.version }}
          tags: |
            kbuley/godevcontainer:${{ steps.vars.outputs.version }}
            kbuley/godevcontainer:${{ steps.vars.outputs.alpine_version }}
            quay.io/kbuley/godevcontainer:${{ steps.vars.outputs.version }}
            quay.io/kbuley/godevcontainer:${{ steps.vars.outputs.alpine_version }}
          push: true
